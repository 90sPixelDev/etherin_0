using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HBSlotUI : MonoBehaviour
{
    //private List<ItemSlot> hbSlots = new List<ItemSlot>();
    public List<UIItemSlot> hbUIslots = new List<UIItemSlot>();


    // Start is called before the first frame update
    void Start()
    {
        //for (int i = 0; i < 9; i++)
        //{
        //    hbUIslots.Add(transform.GetChild(i).gameObject.GetComponent<UIItemSlot>());

        //    hbSlots.Add(new ItemSlot());

        //    hbSlots[i].AttachUI(hbUIslots[i]);
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
